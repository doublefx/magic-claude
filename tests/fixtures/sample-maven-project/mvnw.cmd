@echo off
REM Maven Wrapper script for Windows (stub for testing)
REM This is a simplified version for testing purposes only

if "%1"=="--version" (
    echo Apache Maven 3.9.5
    exit /b 0
)

echo [mvnw.cmd] Maven wrapper stub - would execute: mvn %*
exit /b 0
